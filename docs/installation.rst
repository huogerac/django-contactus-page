============
Installation
============

At the command line::

    $ easy_install django-contactus-page

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-contactus-page
    $ pip install django-contactus-page
